# intervals.icu -> Settings -> Developer -> API Key
INTERVALS_API_KEY=your_api_key_here
# Numeric athlete id, e.g. i123456 (Settings page URL). "0" works only with OAuth.
INTERVALS_ATHLETE_ID=i000000
PORT=8787
